---
- name: ADO | Bootstrap access / validation
  hosts: localhost
  gather_facts: false
  tasks:
    - name: ADO | Show selected environment
      ansible.builtin.debug:
        msg: Running bootstrap access validation for env={{ env | default('prod') }}
